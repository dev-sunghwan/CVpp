# PROJECT_BRIEF

## 1. Project Overview

`CV++`는 C++ 기반의 RTSP 스트리밍 및 비디오 분석 파이프라인을 구현하기 위한 프로젝트다.
초기 목적은 Python/OpenCV의 간편한 RTSP 처리 방식을 넘어, 더 낮은 레벨에서 스트리밍을 제어하고
Hanwha Vision 카메라/NVR 환경에 맞는 커스텀 동작을 직접 다룰 수 있는 구조를 만드는 것이다.

이 프로젝트의 핵심 목표는 아래 3가지다.

1. IP 카메라 RTSP 스트리밍을 C++에서 안정적으로 처리
2. Hanwha 카메라/NVR에서 요구하거나 지원하는 Custom Header를 RTSP 요청에 삽입
3. RTSP 메타데이터(ONVIF 계열)를 파싱하고 화면 오버레이로 검증

## 2. Why This Exists

기존 OpenCV `cv::VideoCapture` 기반 접근은 빠르게 실험하기에는 편하지만,
RTSP `PLAY`/`SETUP` 단계에서 필요한 커스텀 헤더 삽입이나 세밀한 네트워크 제어에는 한계가 있었다.

이 한계를 해결하기 위해 현재 구조는 GStreamer를 중심으로 재구성되었다.
특히 `rtspsrc`의 `before-send` 콜백을 활용해, 실제 네트워크로 나가기 직전의 RTSP 메시지에
커스텀 헤더를 삽입할 수 있도록 만든 것이 이 프로젝트의 가장 중요한 기술적 포인트다.

## 3. Current Implementation Status

현재 구현된 범위는 다음과 같다.

- GStreamer 기반 RTSP 파이프라인 구성
- Hanwha 스트림 접속
- Custom Header 삽입
- 비디오 스트림과 ONVIF 메타데이터 스트림 분리 처리
- 메타데이터 내 객체 정보 파싱
- 바운딩 박스 기반 오버레이 출력

현재 파이프라인 개념은 아래와 같다.

```text
rtspsrc -> decodebin -> videoconvert -> appsink
        -> metadata appsink
```

비디오 프레임은 `appsink`를 통해 OpenCV `cv::Mat`으로 가져오고,
메타데이터는 별도로 받아 XML에서 객체 정보를 추출한 뒤 오버레이에 반영한다.

## 4. Recent Fixes

최근 메타데이터 오버레이 관련 이슈를 기준으로 다음 사항을 보완했다.

- 객체가 없어졌는데도 이전 박스가 계속 남는 문제 수정
- 일정 시간 이상 갱신되지 않은 메타데이터는 그리지 않도록 freshness 처리 추가
- `VehicleInfo`, `HumanInfo`, `Class`, `ClassCandidate` 등 다양한 메타데이터 패턴을 더 넓게 파싱하도록 개선
- 라벨 텍스트가 화면 밖으로 밀려 보이지 않는 문제를 줄이기 위해 좌표 보정 추가

## 5. Known Limitations

현재 프로젝트는 동작하는 프로토타입에 가깝고, 아래 한계가 있다.

- RTSP URL 및 일부 옵션이 코드에 하드코딩되어 있음
- 메타데이터 파서가 아직 정규식 기반이라 구조 변화에 취약할 수 있음
- 메타데이터 모니터링/저장 체계가 아직 없음
- 코드가 단일 `main.cpp` 중심이라 기능 확장 시 복잡도가 빠르게 증가할 수 있음
- 자동 테스트 체계가 약하고, 실험용 스크립트 위주로 남아 있음

## 6. Near-Term Direction

가까운 다음 단계는 "메타데이터의 신뢰성 확보"다.

필요한 작업은 아래와 같다.

1. 수신되는 raw XML 메타데이터를 기록하고 모니터링할 수 있는 구조 마련
2. 파싱 결과를 사람이 검증하기 쉬운 형태로 요약 출력
3. 향후 SQLite 등 경량 저장소를 이용해 메타데이터 이력 비교 기반 마련
4. RTSP URL/헤더/지연시간 등의 설정을 외부 입력으로 분리

즉, 다음 단계는 AI 모델 추가보다 먼저 "카메라가 실제로 어떤 메타데이터를 얼마나 안정적으로 보내는지"
확인 가능한 상태를 만드는 데 초점을 둔다.

## 7. Mid-Term Expansion

이 프로젝트의 확장 방향은 단순한 스트리밍 뷰어를 넘어서, 카메라 내장 AI와 외부 CV 모델을 비교/확장하는 것이다.

주요 확장 아이디어는 다음과 같다.

- Hanwha 카메라 자체 AI 결과와 동일 RTSP 스트림에 YOLO, SAM 등의 모델을 적용했을 때의 성능 비교
- 카메라 자체 기능에는 없는 Face Recognition 등의 기능 구현
- 카메라 메타데이터와 외부 모델 결과를 동일 타임라인에서 비교할 수 있는 분석 구조 구축

이를 위해서는 먼저 카메라 메타데이터를 안정적으로 수집하고, 그 품질을 정량적으로 확인할 수 있어야 한다.

## 8. Recommended Next Milestones

1. `main.cpp`를 기능 단위로 분리해 구조 단순화
2. 설정 외부화(`config` 또는 간단한 UI 입력)
3. 메타데이터 raw log + parsed summary 모니터링 기능 추가
4. 메타데이터 저장소(SQLite 후보) 도입 여부 판단
5. 외부 CV 모델 연동을 위한 입력/출력 인터페이스 정리

## 9. One-Line Summary

이 프로젝트는 "Hanwha RTSP 스트림을 C++에서 직접 제어하고, Custom Header와 메타데이터까지 다룰 수 있는
실전형 비디오 분석 기반"을 만드는 작업이며, 다음 핵심 단계는 메타데이터 관측성과 신뢰성을 높이는 것이다.
